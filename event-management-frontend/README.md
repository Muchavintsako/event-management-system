# Event Management Frontend

Scaffolded Vite + React frontend for Event Management.

To run:

```bash
cd event-management-frontend
npm install
npm run dev
```

The frontend expects the backend at `http://localhost:8080/api`.
